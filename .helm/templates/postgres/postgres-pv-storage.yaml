apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-k121bot-storage
  namespace: {{.Values.namespace}}

  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"

